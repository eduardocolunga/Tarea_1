function setup() {
  createCanvas(400, 400);
  background(0);

fill('red'); // Use 'redValue' in new fill
circle(200, 200, 180); // Draw right rectangle

fill(255);
arc(200, 200, 135, 135, radians(200), radians(369), CHORD);
  
fill(255); // Use color variable 'c' as fill color
arc(200, 200, 135, 135, radians(17), radians(192), CHORD);
}
